%nav.navbar.navbar-default.navbar-square{:role => "navigation"} 
  .navbar-header
    %button.navbar-toggle{:type => "button", :data => { :toggle => "collapse", :target => "#main-nav"} }
      %span.sr-only
        Toggle Navigation
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
    %a.navbar-brand{href: "/"}
      Realtor Minis
  .navbar-collapse.collapse#main-nav
    %ul.nav.navbar-nav
      %li{:class => "#{'active' if current_page?(root_path)}"}= link_to "Home", '/'
      %li{:class => "#{'active' if current_page?(page_about_path)}"}= link_to "About", '/page/about'
      %li{:class => "#{'active' if current_page?(page_pricing_path)}"}= link_to "Pricing", '/page/pricing'
      %li{:class => "#{'active' if current_page?(page_contact_path)}"}= link_to "Contact", '/page/contact'
    %ul.nav.navbar-nav.navbar-right
      - if user_signed_in?
        %li
          = link_to('Logout', destroy_user_session_path, :method=>'delete')
      - else
        %li
          = link_to('Login', new_user_session_path)
      - if user_signed_in?
        %li
          = link_to(current_user.email, edit_user_registration_path)
        - if current_user.admin?
          %li
            = link_to('Admin', users_path)
      - else
        %li
          = link_to('Sign up', new_user_registration_path)
            