Site Links: 
- if listing.active
  %button.btn.btn-primary.add_custom_domain{type: "button", data: {toggle: "modal", target: "#add_new_custom_domain_modal", listing_id: "#{listing.slug}", user_id: "#{@user.id}"}}
    Add new Custom Domain
  
  - listing.domain_names.each do |domain_name|
    =render partial: 'listings/index/domain_name', local: {domain_name: domain_name}
    
  = link_to "//#{listing.full_web_address}", class: 'listing-link btn btn-link', target: "_blank" do
    %i.glyphicon.glyphicon-export
    %span.listing-link= listing.full_web_address
- else 
  %span.btn.btn-link N/A