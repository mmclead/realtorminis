%button.btn.btn-primary.disabled
  You are live!
= link_to 'Publish Changes', publish_listing_path(listing), remote: true, method: :patch, id: 'publish_button', class: 'btn btn-default', :data => { :confirm => 'Are you sure?' }